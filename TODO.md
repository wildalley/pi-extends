# Pi Extends TODO

> 更新时间：2026-08-10
>
> 原则：先处理会破坏只读承诺、覆盖用户数据或造成跨会话串状态的问题；再补齐运行时能力，最后做结构重整与体验增强。

## P0 - 安全与数据完整性

- [x] **收紧 Plan 模式的 Bash 只读判定**
  - 问题：当前允许列表仍放行 `git diff --output=...`、`sort -o ...`、`env python -c ...` 等可写文件或执行任意代码的组合。
  - 动作：改成命令级参数策略；禁止命令包装器和执行型参数，补充 Git/文本工具的输出文件参数拦截。
  - 验收：上述绕过全部被拒绝，既有只读命令仍通过，安全测试覆盖每个回归样例。

- [x] **Plan 模式默认禁用未知自定义工具**
  - 问题：目前只摘掉 `edit`/`write`，其他扩展注册的部署、数据库或文件工具仍可调用。
  - 动作：Plan 模式只保留内置只读工具与显式配置的额外只读工具；未知工具默认关闭，并精确恢复进入前的工具集合。
  - 验收：自定义工具不会在 Plan 模式中静默保留，退出后完整恢复。

- [x] **保护 `/config generate`**
  - 问题：命令路径会无确认覆盖 `.pi/pi-extends.json`，且两条生成路径都绕过项目写入的信任检查。
  - 动作：统一生成逻辑；未信任项目拒绝写入，已有文件默认拒绝覆盖，只有显式 `--force` 或 TUI 二次确认才覆盖。
  - 验收：未信任、已存在、首次生成、强制覆盖四条路径均有测试。

## P1 - 运行时正确性

- [x] **重置 Advisor 的会话级状态**
  - 问题：`shown`、临时开关和运行锁是模块全局状态，切换会话/项目后仍可能沿用。
  - 动作：在 `session_start` 重置会话计数和临时覆盖；会话切换时不让旧旁审结果写入新会话。
  - 验收：两个连续会话各自拥有独立上限与配置开关。

- [x] **从首个未完成步骤继续 Plan**
  - 问题：重新执行部分完成的计划时仍从第 1 步开始，并把已完成步骤列入“剩余步骤”。
  - 动作：只选择和展示未完成步骤；全部完成时直接收尾。
  - 验收：完成第 1 步后重新执行会从第 2 步开始。

- [x] **让十条路由真正接入运行时**
  - 动作：新增统一的 registered/available/capability 解析层；Vision、Advisor、Plan 和子代理复用它，子代理支持可选 `route`（默认 `task`）；增加 `/route <name>` 主会话切换入口。
  - 验收：每条公开路由都可由 `/route` 直接消费，`vision` 额外检查 `image` 能力；未注册、未认证和能力不匹配的候选沿 fallback 继续，并在切换/启动/诊断消息中说明跳过原因。

- [x] **完善自定义厂商生命周期**
  - 动作：支持编辑、连接测试、重复 ID 检查、内置厂商覆盖确认、注册与配置写入失败时回滚。
  - 验收：配置文件与当前 registry 不会出现一边成功一边失败的半完成状态。

- [x] **完善自定义模型元数据**
  - 动作：向导支持 `input: [text,image]`、context window、max tokens、输入/输出/cache read/cache write 价格。
  - 验收：自定义视觉模型能被 vision 路由识别，footer 不再把未知价格误显示为免费。

## P1 - 第三方中转与缓存

- [x] **区分“缓存未命中”和“中转未上报缓存指标”**
  - 现状：footer 读取模型响应里的 `cacheRead`/`cacheWrite`；中转不返回这些字段时只能看到长期零值，无法证明服务端一定没缓存。
  - 动作：增加缓存观测状态 `命中 / 零命中 / 未知`，避免把“未上报”直接等同于“未缓存”。
  - 验收：支持指标的中转显示命中率；不支持指标的中转显示 `CH?`，达到高成本阈值时提示“未上报或未命中”。

- [x] **允许为自定义中转声明缓存能力**
  - 动作：配置中增加 `metrics`、`anthropicCacheControl` 与 `supportsLongRetention`；只映射 Pi 已支持的兼容字段，不接受或伪造任意缓存请求头。
  - 验收：Schema、向导、注册逻辑和文档一致；未知中转保持保守行为。

- [x] **增加中转缓存诊断页**
  - 动作：展示最近若干轮 input/cacheRead/cacheWrite、命中率、provider/model、是否由中转上报。
  - 验收：用户可以区分“Pi 没发缓存提示”“中转忽略提示”“中转命中了但没回传统计”三类情况；无法确定时明确标为未知。

## P2 - 子代理、成本与可观测性

- [ ] **限制 Chain 上一步输出传递量**
  - 动作：默认传摘要或截断文本，大结果保存为 artifact 引用；增加单任务与单批次内存预算。

- [ ] **增加持久任务中心**
  - 动作：任务 ID、列表、取消、失败重试、从失败 chain 步骤继续、会话恢复。

- [ ] **增加预算治理**
  - 动作：Goal、Advisor、子代理分别配置 token/费用/时间预算；达到阈值自动暂停而不是只提示。

- [ ] **增加 `/diagnostics`**
  - 动作：集中展示配置层、当前/配置模型、有效路由、认证、缓存指标、通知通道和子进程健康状态。

- [ ] **持久化 Footer/TPS 偏好**
  - 问题：当前开关仅保存在模块内存，重启后恢复默认。

- [ ] **统一非 TUI 模式行为**
  - 动作：`print/json` 输出纯文本，RPC 才允许交互对话框，避免无 UI 模式调用 `select`。

## P3 - 架构与发布工程

- [ ] **按领域拆分大模块**
  - `cockpit.ts` 拆成外观、模型、工作流、自动化、系统菜单；`ui-kit.ts` 保持纯组件层。

- [ ] **统一配置事实来源**
  - 动作：以 TypeBox Schema 派生 TypeScript 类型与 JSON Schema，减少类型、sanitize、schema、示例四份定义漂移。

- [ ] **串行化配置写入并检测外部修改**
  - 动作：按文件建立写队列；写前校验 mtime/hash，避免并发或手工编辑被最后写入覆盖。

- [ ] **建立兼容性 CI**
  - 动作：测试最低支持 Pi 与最新版、Node 20/22、类型检查、测试、打包和临时目录安装。

- [ ] **移除非必要的 `lucide-static` devDependency**
  - 原因：本地体积约 61 MB；码位校验已有按需拉取回退，不应让每个 clone 默认安装。

## 已完成基线

- [x] Plan/Goal 状态从当前会话分支恢复，不读取废弃分支。
- [x] 子代理默认工具权限显式下传，Plan 关卡按实际工具集判断。
- [x] Footer 用量按当前分支统计，支持缓存命中率、零命中告警与中转指标未知态。
- [x] TUI 卡片宽度、tab、鼠标、主题对比度和图标字形有自动测试。
